#include "Goal.h"

Goal::Goal(double targetWeight, int targetStepCount, int targetWorkoutFrequency, double targetCalorieIntake) :
    targetWeight(targetWeight), targetStepCount(targetStepCount), targetWorkoutFrequency(targetWorkoutFrequency), targetCalorieIntake(targetCalorieIntake) {}
